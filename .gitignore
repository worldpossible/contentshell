# The database will be generated by admin/modules.php when the user saves.
# If that is never done, the modules will just show up alphabetically.
admin/admin.sqlite
# This is the old location, but we want to avoid stray copies getting in.
admin.sqlite
